<div class="container mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-4">編集画面</h1>

  <%# model: @post に「すでにあるデータ」が入っていると、Railsが自動で「更新モード」にしてくれる！ %>
  <%= form_with model: @post, local: true do |f| %>
    
    <div class="mb-4">
      <%= f.text_field :content, class: "border p-2 w-full rounded" %>
    </div>
    
    <div class="flex gap-4">
      <%= f.submit "更新する", class: "bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 cursor-pointer" %>
      
      <%# 「戻る」ボタン %>
      <%= link_to "戻る", root_path, class: "text-gray-500 underline py-2" %>
    </div>
    
  <% end %>
</div>